# Binaries
server
groovebox
*.exe
*.exe;
*.dll
*.so
*.dylib

# music.db is 3-2-1 backed up (restic: Hetzner nightly + local HDD) —
# see homeserver-infra/docs/groovebox-backups.md. No longer in git.
music.db
# Transient WAL-mode artifacts are not the persisted data.
*.db-journal
*.db-wal
*.db-shm

# OS / IDE
.DS_Store
.vscode/
.idea/

# Environment / Credentials
.env
.env.*

# Screenshots
*.png

.tidal-session.json
