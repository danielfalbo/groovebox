# Binaries
server
groovebox
*.exe
*.exe;
*.dll
*.so
*.dylib

# music.db is intentionally git-tracked (the actual library data).
# Transient WAL-mode artifacts are not — they're not the persisted data.
*.db-journal
*.db-wal
*.db-shm

# OS / IDE
.DS_Store
.vscode/
.idea/

# Environment / Credentials
.env
.env.*

# Screenshots
*.png

